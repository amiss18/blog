{% extends 'base.html.twig' %}


{% block head %}
<title>{% block title %}
    {{ post.titre }}{% endblock %}</title>

{% endblock %}

{% block body %}
<div class="container">
</div>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3">
  <a class="navbar-brand" href="{{ path('home') }}">Accueil</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      
      <li class="nav-item active">
        <a class="nav-link" href="{{ path('dashboard') }}">Tableau de bord<span class="sr-only">(current)</span></a>
      </li>

      
    </ul>
    <!--<form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form>-->
  </div>
</nav>


        <div class="container">
<div class="row">
<div class="col-lg-10"> 
    {{ post.titre }} 
    


    <p class="mt-5">{{ post.text }}</p>

    
    <a href="{{ path('post.edit', {id: post.id, slug: post.slug}) }}" class="btn btn-secondary">Editer</a>
</div>
<div class="col-sm-2"><p>Etat {{ post.publieType }}</p></div>
</div>
</div>

{% endblock %}
